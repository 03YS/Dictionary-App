<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar"> 
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/pic1</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <!-- Theme for MainActivity -->
    <style name="MainActivityTheme" parent="Base.Theme.MyApplication">
        <item name="android:windowBackground">@drawable/pic3</item>
    </style>

    <!-- Theme for WordMeaningActivity -->
    <style name="WordMeaningActivityTheme" parent="Base.Theme.MyApplication">
        <item name="android:windowBackground">@drawable/pic3</item>
    </style>

</resources>